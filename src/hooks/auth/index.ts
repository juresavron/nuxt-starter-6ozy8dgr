/**
 * Export all authentication-related hooks
 */

// Export main hooks
export { useAuth } from './useAuth';
export { useRoleCheck, checkUserRole } from './useRoleCheck';

// Export types
export * from './types';